image_size: 224
batch_size: 64
val_frac: 0.1
pretrained_epochs: 40
scratch_epochs: 150
early_stop_patience: 8
lr: 0.0003
warmup_epochs: 3
weight_decay: 0.05
num_workers: 8          # naikkan ke (vCPU - 2) di pod; lihat README Langkah 4
amp: true
